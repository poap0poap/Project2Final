#ifndef SORT_H
#define SORT_H

#include "PlayerInfo.h"    // this brings in the definition of `playerInfo`

// Prototype must use the correct type!
// Here we take the count and an array of playerInfo.
void endGame(int numPlayers, playerInfo playerData[]);

#endif // SORT_H
